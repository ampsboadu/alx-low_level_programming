#include "main.h"
/**
 * mul -multiply two numbers
 * @a: first number
 * @b: second number
 * Return: result of the func
 */

int mul(int a, int b);
{
	int result;

	result = a * b;

	return (result);
}
